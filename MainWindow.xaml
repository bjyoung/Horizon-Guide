<Window x:Class="Horizontal_Guide.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Horizontal_Guide"
        mc:Ignorable="d"
        Title="Horizon Guide" Height="450" Width="800" 
        WindowState="Maximized" SizeToContent="Manual"
        AllowsTransparency="True" WindowStyle="None">

    <Window.Background>
        <SolidColorBrush Opacity="0" Color="White"/>
    </Window.Background>
    
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <Slider x:Name="line_height_slider" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Width="400
                " RenderTransformOrigin="0.5,0.5"  Cursor="None">
            <Slider.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="90"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Slider.RenderTransform>
        </Slider>

        <Canvas Height="Auto" Width="Auto">
            <Line
                X1="10" Y1="60"
                X2="1000" Y2="60"
                Stroke="White"
                StrokeThickness="4" />
        </Canvas>
    </Grid>
</Window>
